describe("Decimal to binary", () => {
  const decimalToBinary = (integer) => {};

  it("should return a valid binary value", () => {
    expect(decimalToBinary(2)).toBe(10);
    expect(decimalToBinary(9)).toBe(1001);
  });
});
